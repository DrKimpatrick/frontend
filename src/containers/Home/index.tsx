import React from 'react';

function Home({ label }: any) {
  return (
    <div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
      <div data-testid="Home">home container{label}</div>
    </div>
  );
}

export default Home;
